{
  "name": "remote-agent",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "node",
  "containerEnv": {
    "GH_CONFIG_DIR": "/home/node/.config/gh"
  },
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "mounts": [
    "source=${localEnv:HOME}/.codex/auth.json,target=/home/node/.codex/auth.json,type=bind",
    "source=${localEnv:HOME}/.config/gh,target=/home/node/.config/gh,type=bind"
  ],
  "initializeCommand": "bash .devcontainer/scripts/ensure-host-codex-auth.sh '${localEnv:HOME}/.codex/auth.json' && bash .devcontainer/scripts/ensure-host-gh-config.sh '${localEnv:HOME}/.config/gh'",
  "postCreateCommand": "corepack pnpm install --frozen-lockfile",
  "forwardPorts": [1355],
  "portsAttributes": {
    "1355": {
      "label": "Portless proxy"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["oxc.oxc-vscode", "openai.chatgpt", "TypeScriptTeam.native-preview"]
    }
  }
}
